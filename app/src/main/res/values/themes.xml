<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.PulsePlanner" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#FF5722</item> <!-- Deep Orange -->
        <item name="colorPrimaryVariant">#E64A19</item> <!-- Darker shade of Deep Orange -->
        <item name="colorOnPrimary">#FFFFFF</item> <!-- White text on Deep Orange -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#4CAF50</item> <!-- Green -->
        <item name="colorSecondaryVariant">#388E3C</item> <!-- Darker shade of Green -->
        <item name="colorOnSecondary">#000000</item> <!-- Black text on Green -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Specify window content transition -->
        <item name="android:windowEnterTransition">@android:transition/fade</item>
        <item name="android:windowExitTransition">@android:transition/fade</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Other styles -->
    <style name="Theme.PulsePlanner.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PulsePlanner.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PulsePlanner.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
